# Cloudflare cache rules (Workers Static Assets).
# Every path rule MUST start with "/". Content-hashed Astro assets — including
# optimized .webp images, which Astro emits into /_astro/ — cache hard for a year.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
